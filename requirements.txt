# Core dependencies
pyyaml>=6.0
pandas>=2.0.0
pyarrow>=12.0.0
requests>=2.28.0

# LLM Providers
google-generativeai>=0.3.0

# HuggingFace
huggingface_hub>=0.19.0
datasets>=2.14.0

# Language detection
langid>=1.1.6

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Optional - for quality scoring
scikit-learn>=1.3.0

# IANA timezone database — bundled on Linux/Mac, missing on Windows.
# Required by zoneinfo.ZoneInfo("America/Los_Angeles") which the Gemini
# provider uses to compute the daily quota reset moment (Pacific midnight).
tzdata; sys_platform == "win32"
